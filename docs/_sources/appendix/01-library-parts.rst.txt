Library Parts
#############

Several of the |OSC| code files we will develop are useful in other projects, so they are collected into a library folder rather than just placinf then in the general model design folders. |OSC| supports loading files from user libraries with a little setup.
